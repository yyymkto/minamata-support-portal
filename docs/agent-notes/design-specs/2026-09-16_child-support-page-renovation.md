# [2026-09-16] 子育て支援制度タブ 改装 設計書

**設計者**: Claude Code
**実装担当**: Antigravity（UI）／ Claude Code（データ調査・JSON作成）
**状態**: UI・データとも実装済み（2026-09-17、未push）。残りはタスク10（ブラウザでの全体確認）

## 概要

「子育て支援制度」タブを、**「水俣市で受けられる子育て支援が一覧できる場所」**として
改装する。吉野さんの意図は「困っている人が探す場所」だけでなく、
**「水俣にはこういう支援がある」と示せる場所にしたい**というもの。

そのため次の3軸で進める。

1. **全体像を冒頭に示す**。タブの先頭に、水俣市の子育て支援を代表する事実を数件
   並べる（例：医療費は18歳まで自己負担なし）。個別の制度を探す前に、
   全体の手厚さが伝わる状態にする。
2. **説明文を事実寄りに書き直す**。現在の説明文は制度の仕組みを中立に述べるだけで、
   一番強い事実が埋もれている。後述の「アピールの方針」に沿って書き換える。
3. **掲載制度の件数を増やす**（現在27制度）。市サイトの子育て関連6カテゴリ＋学校教育・
   給食カテゴリを洗い出した結果、未掲載の有力候補が10件以上見つかった。特に
   **予防接種が1件も載っていない**、**奨学金が就学援助1件しかない**、
   **発達支援の窓口が無い**、**給食費支援が全く無い**という4つの穴が明確になった。

あわせて、現在のトーン（生成り色＋明朝見出し＋藍緑アクセント）は維持したまま
情報の階層を整理する。**折りたたみ・検索機能の追加は今回のスコープ外**とする
（「長すぎて辿り着けない」問題は別の設計書で扱う。下記「設計判断の理由」参照）。

## アピールの方針（この設計書の前提）

2026-08-26に「誘導的・勧誘的な言い回しは避けたい」と決め、タブ名を
「使える制度をさがす」から「子育て支援制度」に変えた経緯がある
（`decisions-log/`および`CURRENT_STATUS.md`参照）。今回の「アピールしたい」は
これと矛盾しない形で実現する。

**アピールは形容詞ではなく、事実の強さで行う。**

| 採用しない | 採用する |
|---|---|
| 「水俣は子育てしやすい街です」 | 「医療費は18歳まで自己負担なし」 |
| 「手厚い支援が受けられます」 | 「3〜5歳の保育料は無償」 |
| 「ぜひご活用ください」 | 「所得制限なし」 |

判断基準：**その文が公式ページに書かれた事実の言い換えで済むならOK。
書き手の評価や呼びかけが入るならNG。**

参考として、市自身の移住・定住促進サイトの子育て支援ページ
（`/ijyu/kiji0031921/`）は「18歳まで（18歳に達してから最初の3月31日まで）
医療費の自己負担がありません」を筆頭の売りにしている。市が何を強みと
考えているかの手がかりになる。

## データ構造

`public/data/child_support_base.json` と `public/data/concern_mapping.json` に
**追記のみ**を行う。既存スキーマの変更はしない。

### 追加する制度の候補（10件）

水俣市サイトの子育て関連カテゴリ（list00730/00736/00737/00738/00739/00740）を
全件確認して抽出。**各ページの内容はまだ個別検証していないため、実装前に
Claude Codeが1件ずつ開いて確認する**（下記タスク1）。

| # | 制度名 | URL(kiji) | 想定カテゴリ | 補足 |
|---|---|---|---|---|
| 1 | 個別予防接種について | kiji0033683 | 妊娠・出産 | 定期予防接種の案内。現在このサイトに予防接種の情報が皆無 |
| 2 | 子宮頸がん予防接種（HPVワクチン） | kiji0033255 | 妊娠・出産 | 小6〜高1女子が対象。子育て世帯に直結 |
| 3 | 市外・県外で予防接種を希望する人へ | kiji0034276 | 妊娠・出産 | 里帰り出産・転出入時に必要 |
| 4 | 水俣市奨学金（貸付型） | kiji003130 | お金・手当・医療費 | 高校生・大学生の子を持つ世帯向け |
| 5 | 松本眞一同朋奨学金（給付型） | kiji003263 | お金・手当・医療費 | 同上。給付型なので優先度高 |
| 6 | 水俣市こどもセンター | kiji003260 | 預ける・学校 | 児童館的な居場所。2026-09-08更新と新しい |
| 7 | 認定こども園で実施している学童クラブ | kiji003285 | 預ける・学校 | 学童の別形態。現在は学校学童1件のみ |
| 8 | 水俣芦北圏域発達支援パンフレット | kiji0034262 | 特別なサポート | 発達支援の案内冊子（info_type=guide想定） |
| 9 | 水俣芦北圏域地域療育センター | kiji003211 | 特別なサポート | 療育の拠点。ただし最終更新2015年（要確認） |
| 10 | 父母の離婚後の子の養育に関する民法等改正 | kiji0034376 | 特別なサポート | ひとり親向けの法改正案内（info_type=guide想定） |

### タスク1の検証結果（2026-09-17、Claude Code）

10件すべての実ページ本文を取得して確認した。**8件を掲載、2件を見送り。**

| # | 判断 | base_id | ページ最終更新日 | 備考 |
|---|---|---|---|---|
| 1 | 掲載 | `routine-vaccination` | 2026-07-31 | R8.4から妊婦のRSウイルスワクチンも定期接種。自己負担の有無はページに記載が無いため書いていない |
| 2 | 掲載 | `hpv-vaccination` | 2026-04-02 | キャッチアップ接種はR8.3.31で終了済み |
| 3 | 掲載 | `vaccination-outside-city` | 2026-09-03 | 事前申請なしで県外接種すると全額自己負担、を条件欄に明記 |
| 4 | 掲載 | `city-scholarship-loan` | 2025-12-26 | タイトルに「令和８年度」が入るが常設ページが別に無い。kiji番号が古く、毎年書き換えて使っていると判断 |
| 5 | 掲載 | `matsumoto-scholarship-grant` | 2025-10-01 | 同上。「返済不要」はページに明記が無いため使っていない |
| 6 | 掲載 | `kodomo-center` | 2026-09-08 | 通信PDFの差し替えで毎月更新されるため、監視で「更新の可能性あり」が出やすい |
| 7 | **見送り** | — | 2019-11-29 | 同じ民間学童クラブが `gakudo-club`（kiji0033506）の「その他の水俣市内の学童クラブ」欄に最新の連絡先つきで載っている。既存項目の説明文に民間学童の存在を追記して対応 |
| 8 | 掲載 | `development-support-pamphlet` | 2025-06-25 | 冊子PDF本文も確認。`household_type` は空（「発達が気になる」段階の世帯が「障がい児」を選ばなくても出るように） |
| 9 | **見送り** | — | 2015-03-26 | 同ページのPDFの所在地・電話（平町1-3-3、0966-84-9540）が、2025年版パンフレットでは「児童発達支援センターにこにこ」として載っており、「地域療育センター」の名称は出てこない。現行の窓口は#8の条件欄で案内 |
| 10 | 掲載 | `post-divorce-parenting-law` | 2026-02-18 | 設計時は困りごとの紐づけ先が未定だったため、既存の `single-parent-support` に追加 |

**困りごと（タスク3）で設計から変えた点**

- `vaccination` を新設するにあたり、既存の `infant-health-check` の見出し
  「赤ちゃん・子どもの健診や予防接種について知りたい」から「や予防接種」を外した。
  同じ語の見出しが2つ並ぶのを避けるため。
- `child-development-concern` の紐づけは、設計時の「#8,#9＋`medical-care-child-guideline`」から
  「#8＋`child-family-center`」に変えた。#9は見送り、医療的ケア児ガイドラインは
  「発達が気になる」とは対象が違う（人工呼吸器・胃ろう等）ため。
- `scholarship` には市の奨学金2件に加え、修学資金を含む `single-parent-welfare-loan` も紐づけた。
- `priority` は既存を振り直さず、各カテゴリの末尾（4, 105, 206, 305）に追加した。
  UIは `priority` ではなく配列順で並べているため、配列上も各カテゴリの末尾に挿入した。
- 既存の `pregnancy-checkup-cost` に、`items` に存在しない `birth-childcare-support-grant`
  への参照が残っていたので除去した（UIでは `filter(Boolean)` で無視されていた）。

**タスク4の結果**

- `sick-child-care`：kiji003384 に差し替えた。ただし常設ページは利用日を「月〜土」と
  書いたままで、告知ページ（kiji0034513）でR8.1.11から「月〜金」に変更済み。
  条件欄は告知側に合わせ、その旨を注記した。
- `gakudo-club`・`child-allowance`：市サイトのカテゴリ（list00730/00731/00741 ほか）の
  全記事を確認し、常設ページが無いことを確かめた。URLは維持。
- **`child-allowance` の説明文は改正前の内容（「中学生まで」「所得制限等の条件があります」）の
  ままで、公式ページ（所得制限撤廃・高校生年代まで）と逆になっていた。** 事実に合わせて書き換えた。
  「アピールの方針」の観点でも、所得制限なし・第3子以降月3万円は強い事実。

**調査中に見つけた、候補10件以外の未掲載ページ（2026-09-17、吉野さんの了承を得て3件とも追加済み）**

| 制度名 | kiji | 補足 |
|---|---|---|
| 水俣市不妊治療費助成について | kiji0031656 | 妊娠・出産カテゴリ（list00749）。2026-04-01更新 |
| 母子家庭等高等職業訓練促進給付金 | kiji0034082 | ひとり親。掲載済みの自立支援教育訓練給付金と対になる制度 |
| みなまた子育てガイドブック | kiji0033559 | 案内冊子（guide向き） |

### 給食費支援（最優先・事実は確定済み／掲載方法は未決定）

**2026-09-16、吉野さん提供の一次資料により内容を確定した。**

出典：水俣市議会だより No.106（令和8年5月1日号）「令和8年度補正予算を可決」、
および令和8年度一般会計補正予算書（第1号）(款)9教育費 (項)5保健体育費
(目)5給食施設費。

| | 小学校 | 中学校 |
|---|---|---|
| 事業名 | 学校給食費補助事業 | 水俣市学校給食費補助事業 |
| 予算額 | 6,304万円 | 3,088万円（予算書：30,885千円） |
| 内容 | 市内小学校に在籍する**児童について給食費の全額を補助** | 市内中学校に在籍する**生徒について給食費の全額を補助** |
| 国との関係 | 国からの交付金で**不足する分は市が支出** | 国の制度の対象外＝**全額市費（市独自）** |

予算書側の裏付け：節18「負担金、補助及び交付金」30,885千円、説明欄に
「水俣市学校給食費補助金」と明記。補正額の財源内訳は全額が一般財源。

**確定した事実：小中学校とも給食費は実質無料。中学校分は国の制度の対象外であり、
水俣市独自の措置。** 対象の書き方は「市内小学校に在籍する児童について」
「市内中学校に在籍する生徒について」で、所得制限の記載はない。

**アピール材料としての価値が最も高い一件である。** 国の制度（2026年4月開始）は
公立小学校のみが対象のため、中学校まで全額補助している点は他自治体との差別化になる。
にもかかわらず、**市の公式サイトには給食費支援の制度ページが存在しない**
（「給食」カテゴリ（list00743）は給食だより・献立カレンダー・食育だよりの3件のみ）。
市民が公式サイトを探しても辿り着けない情報であり、このサイトが埋めるべき穴として
最も分かりやすい。

#### 決定1：`permanence` は `ongoing`、ただし年度を明記する（2026-09-16 吉野さん判断）

補正予算による事業のため単年度か恒久かは資料から読み取れないが、情報としての
価値を優先して掲載する。ただし**年度を明記して、いつ時点の話かが必ず分かる形に
する**（「給食費は無料です」ではなく「令和8年度は全額補助」と書く）。

- `description`・`conditions_note`に年度を必ず入れる。
- 年度が変わったら再確認が必要な項目として、`verification_status`にその旨を書く。
- `scripts/monitor_stock.py`は`official_url`のページ更新を監視する仕組みなので、
  リンクを持たないこの項目は監視対象外になる。**年度替わりの確認は人手で行う**
  必要がある（下記「運用上の注意」）。

#### 決定2：`official_url` を持たないカードを許容する（2026-09-16 吉野さん判断）

市の公式サイトに制度ページが無いため、リンクなしのカードを認める。
これは**スキーマとUIの両方の変更**を伴う。

### スキーマ変更：`official_url` の任意化と `source_note` の追加

現在、全27件が`official_url`必須を前提としている。給食費のように公式ページが
存在しない制度のために、次の2点を変更する。

```json
{
  "base_id": "school-lunch-subsidy",
  "title": "学校給食費の全額補助",
  "official_url": null,
  "source_note": "水俣市議会だより No.106（令和8年5月1日号）／令和8年度一般会計補正予算書（第1号）",
  ...
}
```

- `official_url`: **`null` を許容する**（既存27件は変更しない）。
- `source_note`: **新設。`official_url`が`null`のときのみ必須**。出典の名称を書く。
  URLではなく人が読む文字列。公式ページがある制度では書かない（または省略）。

**`official_url`が`null`の項目は、出典が公式サイト外であることを意味する。**
この状態を安易に増やさないため、次を原則とする。

> 公式サイトに制度ページが存在することを確認した上で、それでも無い場合にのみ
> `official_url: null` を使う。「探すのが面倒だから」は理由にならない。

### 意図的に掲載しないもの

| 制度名 | URL(kiji) | 理由 |
|---|---|---|
| 物価高対応子育て応援手当 | kiji0034516 | `permanence=time_limited` に該当。スキーマ方針により静的データからは除外し、`life_info.json`側で拾う |
| 水俣市子ども・子育て支援事業計画 | kiji003262 | 行政計画であり、住民が申請・利用する制度ではない |
| 保育施設の休園基準／緊急安全点検／コロナ対応 | kiji0032253 ほか | 制度ではなく運用連絡。2020〜2023年で情報が古い |

### 既存3件のURL差し替え検討（重要）

調査中に、**恒久的な制度の説明ページではなく、年度限定の告知ページを
`official_url`に登録している項目**が3件見つかった。年度が変わると内容が
差し替わる、あるいはページ自体が消える構造的リスクがある。

| base_id | 現在のURL | 現在のページタイトル | 差し替え候補 |
|---|---|---|---|
| `sick-child-care` | kiji0034513 | 病児・病後児保育事業 **からのお知らせ** | kiji003384「病児・病後児保育事業」（常設の事業説明ページが別に存在） |
| `gakudo-club` | kiji0033506 | **令和８年度**学校学童クラブ利用児童の募集について | 恒久ページが見当たらない。要調査 |
| `child-allowance` | kiji003155 | **令和6年10月から**児童手当の制度を拡充します。 | 恒久ページが見当たらない。要調査（`verification_status`に既に課題として記録済み） |

### 困りごと（concern）の追加

`concern_mapping.json` に4件追加する想定（現在16件 → 20件）。

| concern_id | label | category_id | 紐づくbase_id |
|---|---|---|---|
| `vaccination` | 予防接種について知りたい | pregnancy | 上記1,2,3 |
| `scholarship` | 高校・大学の学費（奨学金）について知りたい | money | 上記4,5 |
| `child-development-concern` | 子どもの発達が気になる | special | 上記8,9（＋既存 medical-care-child-guideline） |
| `places-to-play` | 子どもの遊び場・居場所を知りたい | childcare | 上記6 |

## 画面仕様

このプロジェクトはAPIを持たないため画面仕様のみ。対象は `src/index.html` の
`#view-concern`（および共通関数 `renderConcernSet` / `concernSectionHTML` /
`serviceCardHTML`）。

**現在のトーン（`paper`/`ink`/`bay` の配色、明朝見出し、白カード）は変更しない。**
変えるのは階層の付け方だけ。

### 0. 冒頭に「全体像」セクションを追加（今回の主役）

`#view-concern` のヘッダー直下、カテゴリ一覧が始まる前に、水俣市の子育て支援を
代表する事実を数件並べる。**個別の制度を探す前に、全体の手厚さが伝わる状態を作る。**

```
　水俣市の子育て支援

　　医療費          18歳まで自己負担なし
　　保育料          3〜5歳は無償
　　給食費          （裏取り後に確定）
　　相談            妊娠期から切れ目なく対応（こども家庭センター）

　　※ 詳しい条件は下の各制度をご確認ください
```

- 掲載は**4〜5件まで**。増やすと「全体像」ではなく「もう一つの一覧」になる。
- 各行は「短い見出し語（医療費・保育料など）＋事実」の2列。**説明文は書かない。**
- 各行は該当する制度カードへのページ内リンクにする（クリックで下の該当箇所へ移動）。
- 色・書体は現在のトーンを踏襲する。**バナー的な装飾（グラデーション・大きな数字・
  アイコンの多用）はしない。** 落ち着いた事実の列として見せる。
- データは `child_support_base.json` に新設する `highlights` 配列から読む
  （下記データ構造参照）。HTMLに直書きしない。

#### `child_support_base.json` への追加（トップレベル）

```json
{
  "_schema_note": "...",
  "last_updated": "2026-09-16",
  "highlights": [
    {
      "label": "医療費",
      "fact": "18歳まで自己負担なし",
      "base_id": "child-medical-subsidy"
    },
    {
      "label": "保育料",
      "fact": "3〜5歳は無償",
      "base_id": "childcare-education-free"
    }
  ],
  "items": [ ... ]
}
```

- `label`: 短い見出し語（4文字程度まで）
- `fact`: 事実の言い切り。公式ページに書かれた内容の言い換えで済む範囲に限る
- `base_id`: リンク先の制度。`items`内に存在する`base_id`であること

`highlights` が存在しない・空配列の場合はセクションごと描画しない
（既存データとの後方互換のため）。

#### 実装上の注意（ここを外すと壊れる）

**(a) 置き場所**：`#view-concern` の `<header>` は `sticky top-12` で常時表示される。
全体像をこの中に入れると**スクロールしても消えず画面を占有し続ける**ので入れないこと。
`</header>` と `<main id="concern-content">` の間に、専用のコンテナを1つ置く。

```html
</header>
<div id="concern-highlights" class="max-w-2xl mx-auto px-4 pt-6"></div>
<main id="concern-content" class="max-w-2xl mx-auto px-4 py-6"></main>
```

**(b) 再描画で消えないようにする**：`renderConcernSet()` は
`contentEl.innerHTML = ...` で中身を丸ごと置き換える。全体像を
`#concern-content` の中に描画すると、プロフィール絞り込みのたびに消える。
**必ず別コンテナ（`#concern-highlights`）に、初期化時に1回だけ描画する。**

**(c) 絞り込みの対象外**：全体像は「水俣市全体でどうか」を示すものなので、
**プロフィール絞り込み（`itemMatchesProfile`）の影響を受けてはいけない。**
「小学生」を選んだら給食費だけが残る、といった挙動にしないこと。

**(d) 「一人暮らし・若者支援」タブには出さない**：`renderConcernSet` は両タブで
共有されているが、`young_adult_support_base.json` に `highlights` は無く、
今回の対象は子育てタブのみ。**全体像の描画は `renderConcernSet` の中に
書かないこと**（共有関数に入れると若者タブにも影響する）。子育てタブ専用の
初期化処理として書く。

### 1. カテゴリ見出しに件数を添える

現在（[index.html:641](../../../src/index.html#L641)）はカテゴリ名のみ。
絞り込み後の実際の表示件数を右側に小さく添える。

```
妊娠・出産                                    6件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

- 件数は `catSections` 計算時に確定する表示対象の制度数（重複を除いた実数）。
- 文字サイズは `text-xs`、色は `text-inkSoft`、`font-normal`（見出しと競合させない）。

### 2. 困りごと見出しの様式を変える（最重要）

現在、カテゴリ見出し（h2, `font-serif font-bold text-xl`）と困りごと見出し
（h3, `font-serif font-bold text-base`）がどちらも明朝の太字で、強弱が弱い。
困りごとは「〜について知りたい」という**問いかけ**なので、見出しというより
語りかけとして差別化する。

- h3を `font-serif font-bold text-base text-ink` から、**左に`bay`の縦罫線
  （`border-l-2 border-bay/40 pl-3`）＋ `text-[15px]` ＋ `font-medium`** に変更。
- 上の余白を広げる（`mt-6`）。カテゴリ内の区切りを視覚的に明確にする。
- 明朝体（`font-serif`）は維持する。

### 3. カード内の情報順を整理

現在（[index.html:526-545](../../../src/index.html#L526-L545)）の順序は
「種別バッジ＋最終確認バッジ → タイトル → 説明 → 警告 → 条件 → CTA」。
最終確認日がタイトルより上にあり、最初に目に入る情報としては優先度が低い。

変更後の順序：

```
[種別バッジ]                     ← 最終確認バッジはここから外す
制度名（明朝・太字）
説明文
（3年超の警告ボックスがあればここ）
（条件があればここ）
[ 申請する → ]
─────────────────
最終確認：2026-07-17            ← 下部へ移動、text-[11px] text-inkSoft
```

- 3年超の警告ボックス（`fresh.level === 'warn'`）は**現在の位置・見た目のまま維持**。
  これは目立つべき情報なので下げない。
- 下部の最終確認は区切り線（`border-t border-line`）の下に置く。

### 3b. リンクを持たないカードの表示（新規・給食費のために必要）

現在の `serviceCardHTML`（[index.html:537-541](../../../src/index.html#L537-L541)）は
必ずCTAボタン（「申請する」等）を描画する。`official_url`が`null`の場合、
`safeUrl()`は`'#'`を返すため、**そのままでは「申請する」を押しても何も起きない
壊れたボタンが出る。**

`official_url` が `null`（または未設定）の場合は次のようにする。

- **CTAボタンを描画しない。**
- 代わりに、カード下部に出典を明示する。

```
[費用の助成]
学校給食費の全額補助
市内の小学校・中学校に在籍する児童生徒の給食費を全額補助しています。
令和8年度は小学校分6,304万円、中学校分3,088万円を予算化。
中学校分は国の制度の対象外で、水俣市独自の措置です。

（条件の注記）令和8年度の補正予算に基づく事業です。年度ごとに
　　　　　　　予算が決まるため、最新の状況は市へご確認ください。
─────────────────
出典：水俣市議会だより No.106（令和8年5月1日号）
　　　※ この制度は市の公式サイトに専用ページがありません
最終確認：2026-09-16
```

- 出典行は `text-[11px] text-inkSoft`。CTAボタンと同じ視覚的重みにしない。
- 「※ この制度は市の公式サイトに専用ページがありません」は固定文言とする
  （`source_note`があるとき常に表示）。**利用者が「リンクが無いのは不具合ではない」
  と分かるようにするため。**
- `guideCardHTML`はカード全体が`<a>`なので、`official_url`が`null`の項目は
  `info_type: "guide"` にしない運用とする（給食費は`service`なので問題ない）。

### 4. 対象ステージの表示（任意・削ってよい）

`child_stage` / `household_type` は絞り込みにしか使われておらず、カード上では
見えない。種別バッジの隣に小さく並べると「自分の子に関係あるか」が一目で分かる。

```
[現金給付] 小学生・中学生
```

- 表示は `child_stage` が6ステージ全部を含む場合は「すべての年代」と省略する
  （27件中10件以上が全ステージ対象のため、そのまま並べると冗長）。
- `household_type` は空でない場合のみ「ひとり親」「障がい児」を表示。
- **この項目だけは吉野さんが選ばなかった方向（情報の追加）に踏み込むため、
  不要なら削ってよい。** 詳細は「設計判断の理由」参照。

### 5. ジャンプナビのスクロール位置の固定値を修正

現在 `scroll-mt-[180px]`（[index.html:640](../../../src/index.html#L640)）が
ハードコードされている。スマホ幅ではプロフィールチップ8個が2〜3行に折り返して
ヘッダーが高くなるため、ジャンプ時に見出しがヘッダーに隠れる。

- ヘッダー要素の実高さを取得してCSS変数（例 `--concern-header-h`）に設定し、
  `scroll-margin-top` をそこから算出する。
- 画面幅の変更時（`resize`）にも再計算する。

**注意：`scroll-mt-[180px]` は `renderConcernSet()` の中にあり、
「一人暮らし・若者支援」タブでも同じ値が使われている。**
しかし若者タブのヘッダーにはプロフィールチップが無いぶん低い。
**タブごとに実高さを測る**こと（両タブに同じ固定値を入れ直すのは修正になっていない）。
なお全体像セクション（仕様0）を追加すると子育てタブのヘッダー下の高さが変わるため、
**仕様0を実装したあとに、この高さ計算を確認する**こと。

## タスク一覧

各タスクは半日程度で完了する単位。

### データ（Claude Code担当）

| # | タスク | 完了条件 |
|---|---|---|
| 0 | ~~給食費支援の一次情報での確定~~ → **完了（2026-09-16、吉野さん提供資料により確定）** | 小中学校とも給食費全額補助であることを議会だよりNo.106と補正予算書で確認済み |
| 0a | ~~給食費の`permanence`と`official_url`の方針決定~~ → **完了（2026-09-16）** | `permanence: ongoing`＋年度明記、`official_url: null`を許容、で決定済み |
| 0c | 給食費の項目を`child_support_base.json`に追加 | `official_url: null`・`source_note`つきで追加され、`description`と`conditions_note`に**年度が明記**されている。`how_to_apply`は資料で確認できた範囲だけを書き、**申請要否を推測で書かない**（不明なら「資料では確認できていない」旨を記す） |
| 0b | 説明文（`description`）の事実化 | **一部完了**：`child-medical-subsidy`（2026-09-16）、`child-allowance`・`sick-child-care`・`gakudo-club`（2026-09-17、タスク4のページ再確認にあわせて）は書き換え済み。新規8件は最初から事実ベースで作成。→ **完了（2026-09-17）**：残り23件も公式ページ本文と担当課（お問い合わせ先）を確認して書き直した。事実の誤りとして、窓口の誤り5件（「福祉課」→こども子育て課）、移行済み給付金の記載、対象外の用具名、last_verified の誤り3件を訂正。評価・呼びかけ語が無いことも機械的に確認済み。「アピールの方針」に沿って書き換え、書き手の評価・呼びかけが入っていないことを全件確認する |
| 1 | ~~追加候補10件の実ページを1件ずつ開いて内容検証~~ → **完了（2026-09-17）**。結果は「タスク1の検証結果」参照 | 各ページの実在・対象者・`last_verified`（ページ最終更新日）を確認し、掲載可否を判断した一覧ができている。掲載見送りの判断にも理由が書かれている |
| 1b | ~~`highlights` 配列の作成~~ → **完了（2026-09-16）** | 4件（医療費・給食費・保育料・相談）を`child_support_base.json`に追加済み。全`base_id`が`items`内に存在することを検証済み。**UI実装時はこのデータで動作確認できる** |
| 2 | ~~`child_support_base.json` に検証済みの制度を追記~~ → **完了（2026-09-17、8件）** | 既存スキーマの全必須フィールド（`base_id`〜`info_type`）が埋まっており、`python -c "import json;json.load(...)"` が通る。既存27件は変更されていない |
| 3 | ~~`concern_mapping.json` に困りごと4件を追記~~ → **完了（2026-09-17）**。設計から変えた点は「タスク1の検証結果」参照 | 追加した`concern_id`の`base_ids`が全て`child_support_base.json`に存在する。`category_id`が`_categories`のいずれかと一致する。`priority`が既存の採番規則（カテゴリごとの連番）に従っている |
| 4 | ~~既存3件のURL差し替え調査~~ → **完了（2026-09-17）** | `sick-child-care`をkiji003384に差し替え。`gakudo-club`・`child-allowance`は恒久ページの有無を調査し、無ければ`verification_status`にその旨を追記して現状維持とする |

### UI（Antigravity担当）

| # | タスク | 完了条件 |
|---|---|---|
| 4a | リンクを持たないカードの表示対応（仕様3b） | **最小限は実装済み（2026-09-16、Claude Code）**。`serviceCardHTML`に`hasLink`の分岐が入り、CTA非描画・出典表示・注記まで動作している（本番反映済み）。**残りは体裁のみ**：最終確認日の位置を仕様3の整理と揃えること。**既存の`hasLink`分岐を壊さないこと**（`safeUrl(null)`は`'#'`ではなく`/null`という404リンクを返すため、分岐を外すと本番で壊れる） |
| 4b | 冒頭の「全体像」セクションの実装（仕様0） | `highlights`から描画され、各行が該当制度へページ内リンクしている。`highlights`が無い・空のデータでもエラーにならず、セクションごと非表示になる。スマホ幅で2列が崩れない |
| 5 | カテゴリ見出しへの件数表示（仕様1） | 各カテゴリ見出しの右に件数が出る。プロフィール絞り込みを変更すると件数も追随する。件数は重複を除いた実数 |
| 6 | 困りごと見出しの様式変更（仕様2） | h3が縦罫線つきの様式になり、カテゴリ見出しとの強弱が明確になっている。「一人暮らし・若者支援」タブも同じ関数を共有しているため、**両タブで崩れていないこと**を確認済み |
| 7 | カード内の情報順の整理（仕様3） | 最終確認日が区切り線の下に移動し、3年超の警告ボックスは従来の位置・見た目のまま残っている |
| 8 | 対象ステージの表示（仕様4・任意） | 全ステージ対象の制度で「すべての年代」と省略表示される。`household_type`が空の制度で余計な表示が出ない |
| 9 | ジャンプナビのオフセット修正（仕様5） | スマホ幅（375px）でプロフィールチップが複数行に折り返した状態でも、ジャンプ先の見出しがヘッダーに隠れない |

### 検証

| # | タスク | 完了条件 |
|---|---|---|
| 10 | 全体の動作確認 | 下記「検証の方法」の4点すべてを満たす |

### 検証の方法

このプロジェクトにはテストの仕組みが無い。最低限、次を確認すること。

**1. ローカルで実際に開く**（本番と同じ構成を組み立てる）

```bash
mkdir -p /tmp/dist/data && cp src/index.html /tmp/dist/index.html \
  && cp -r public/data/. /tmp/dist/data/ && (cd /tmp/dist && python -m http.server 8765)
```

`daily-update.yml` の `build-and-deploy` と同じ組み立て方。`src/index.html` は
`./data/*.json` を参照するため、リポジトリのディレクトリ構成のままでは動かない。

**2. 3タブすべてを開き、コンソールにエラーが出ないことを確認する**

2026-08-24に、参照先のDOM要素が無いまま`innerHTML`に代入して
**サイト全体がエラー画面になる**事故が起きている。子育てタブの変更が
フィードタブ・若者タブを巻き込んでいないことを必ず確認すること。

**3. 描画関数を実データで直接叩く**（ブラウザが使えない場合にも有効）

```bash
node -e "
const fs=require('fs');globalThis.location={href:'https://example.com/'};
const h=fs.readFileSync('src/index.html','utf8');
const g=(s,e)=>{const a=h.indexOf(s);return h.slice(a,h.indexOf(e,a));};
const fn=new Function([g('const ENTRY_TYPE_STYLE','const state ='),g('function esc(v)','function fmtDate'),g('function freshnessNote','function guideCardHTML')].join('\n')+'\nreturn {serviceCardHTML};')();
const d=JSON.parse(fs.readFileSync('public/data/child_support_base.json','utf8'));
for(const i of d.items){const o=fn.serviceCardHTML(i);
  if([...o.matchAll(/href=\"([^\"]*)\"/g)].some(m=>/\/null\$|\/undefined\$/.test(m[1])))console.log('壊れたリンク',i.base_id);}
console.log('OK');"
```

**4. プロフィール絞り込みを操作して確認する**

- 「小学生」だけを選んだとき、カテゴリの件数が追随する（仕様1）
- **全体像セクションは変化しない**（仕様0の注意(c)）
- すべての制度が消える組み合わせ（例：「妊娠中」＋「障がい児」）でも、
  見出しだけの空セクションやエラーにならない

**タスク5〜9はタスク2〜3（データ追加）と独立しているため、並行して進めてよい。**
ただしタスク10は全て終わってから行う。

## 設計判断の理由

### 「アピールしたい」と「勧誘的な表現は避けたい」をどう両立させたか

2026-08-26に吉野さんは、誘導的なコピー案（タブ名「水俣で受けられる支援」等）を
見て「誘導するような文言は省きたい」と判断し、中立的な紹介表現に留めた経緯がある。
今回の「なんならアピールするところにしたい」はその決定の撤回ではなく、
**「事実が弱く書かれすぎていて、あるものが伝わっていない」という別の問題**への
指摘だと解釈した。

実際、`child-medical-subsidy`の説明文は「医療機関受診時の自己負担額の一部または
全部を助成する制度」となっており、市自身が移住サイトの筆頭で掲げている
「18歳まで自己負担なし」という事実が読み取れない。これは中立性の問題ではなく、
単に事実が正確に伝わっていない。

そこで「アピール＝事実の強さ」という基準を置き、形容詞・呼びかけは従来どおり
使わない方針とした。この基準なら2026-08-26の決定と衝突しない。

### 給食費が「一次情報主義」と衝突する点について

本プロジェクトは一次情報（公式ページ）で裏が取れるものだけを載せる方針で
運用してきた。しかし給食費支援は、**市の公式サイトに制度ページが無く、
議会だより（画像PDF）と予算資料にしか存在しない**。

「載せたい情報ほど公式サイトに無い」という状況は今後も起こりうるため、
今回は次の扱いとする。

- **推測では書かない。** 裏が取れなければ載せない（タスク0の完了条件）。
- 公式サイト以外を出典にする場合は、`verification_status`に**出典の種類
  （議会だより第◯号・予算資料等）と確認日を明記する**。通常の`official_url`とは
  性質が違うことが後から分かるようにする。
- `official_url`に適切なページが無い制度をどう扱うか（リンク先を持たないカードを
  許容するか）は、この設計書では決めない。タスク0で裏が取れた段階で改めて相談する。

### なぜ折りたたみ・検索を今回入れないか

吉野さんは改装の主題として「見た目・読みやすさの刷新」と「掲載制度の充実」を
選び、「長すぎて目的の制度に辿り着けない」は選ばなかった。そのため、構造そのものを
変える折りたたみ・検索は今回のスコープから外した。

ただし**制度を10件増やすと、現在の「16の困りごとを常時全展開」という構造では
ページはさらに1.4倍程度長くなる**。今回の階層整理（見出しの強弱・余白・件数表示）は
流し読みの手がかりを増やすので悪化を緩和できるが、根本解決ではない。
制度が40件を超えたあたりで折りたたみか検索が必要になる見込みで、そのときは
別の設計書を起こす。

### なぜ「対象ステージの表示」を任意として入れたか

これは吉野さんが選ばなかった方向（情報の追加）に属する。それでも仕様4として
書いたのは、階層整理の観点で「この制度は自分の子に関係あるか」が最上位の判断で
あり、それが今カード上で分からないため。ただし主題ではないので、判断は委ねる。

### なぜ予防接種を最優先の追加候補としたか

現在このサイトには予防接種の情報が1件も無い。乳幼児健診（`infant-checkup`）は
あるのに予防接種が無いのは、子育て世帯の実際の関心から見て穴が大きい。
また子宮頸がんワクチンは小6〜高1が対象で、現在手薄な「中学生・高校生の
子を持つ世帯」への数少ない接点でもある。

### 既存URLの差し替えを設計書に含めた理由

改装とは直接関係しないが、制度を追加する際に同じ失敗（年度限定の告知ページを
恒久制度として登録する）を繰り返さないため、先に基準を明確にしておく必要がある。
**新規追加時も「年度・改正年月がタイトルに入っているページは恒久ページを探す」を
原則とする。**

## 運用上の注意（この改装で新しく生じるもの）

### リンクを持たない項目は自動監視の対象外になる

`scripts/monitor_stock.py` は各制度の `official_url` にアクセスして、ページの
最終更新日の変化やリンク切れを検知し、週次でIssueを起票する仕組み。
**`official_url` が `null` の項目は、この監視から外れる。**

給食費補助は年度ごとの予算に基づく事業のため、本来もっとも変化を追う必要がある
にもかかわらず、自動監視が効かないという逆転が起きる。対応として次のいずれかが必要。

- **当面**：年度替わり（4〜5月）に人手で議会だより・予算資料を確認する。
  `verification_status` にその旨を明記しておく。
- **将来**：`monitor_stock.py` に「`official_url` が `null` の項目を、
  `last_verified` から一定期間経過したら通知する」仕組みを足す。
  今回のスコープ外だが、`official_url: null` の項目が増えるなら必要になる。

### 「全体像」の事実は古くなる

冒頭のハイライトは、年度や制度改正で変わりうる事実を短く言い切る形になる。
説明文より目立つ位置にあるため、**間違ったまま放置されたときの影響が大きい**。
`highlights` の各項目は、リンク先の制度の `last_verified` を実質的な鮮度の
根拠とみなし、元制度が古くなったらハイライトも見直す運用とする。

## 実装時の疑問点・ブロッカー（実装担当が記入）

### 2026-09-17: 仕様4「すべての年代」の判定基準について（Antigravity追記・解決済み）
- **経緯**: 仕様4で「表示は child_stage が6ステージ全部を含む場合は「すべての年代」と省略する（27件中10件以上が全ステージ対象のため、そのまま並べると冗長）」と記載されていたが、実際の `child_support_base.json` でこれに該当する10件の制度（児童手当・子どもの医療費助成等）は、出生後の5ステージ（`['乳幼児', '未就学', '小学生', '中学生', '高校生']`）を網羅しており、`妊娠中` は含まれていなかった（妊娠中は出生前のため）。
- **対応**: 出生後の5ステージをすべて満たす項目（および6ステージ満たす項目）を「すべての年代」と判定するように実装。これにより、設計書の想定通り対象10件で「すべての年代」と表示され、冗長な羅列が回避されることを確認済み。

**→ 設計担当（Claude Code）確認：2026-09-17 了承。** 設計書の「6ステージ全部」は、
`child_stage` の実データ（出生後の5ステージを網羅した項目に `妊娠中` は含まれない）を
確認しないまま書いた誤りだった。「出生後の5ステージを網羅したら『すべての年代』」という
判定で意図どおり。なお `妊娠中` だけ・`妊娠中`＋一部ステージの項目はこれまでどおり列挙される。

### 2026-09-17: 候補外3件の追加と、制度数が40件の目安に達した件（Claude Code）

- 不妊治療費助成（`fertility-treatment-subsidy`）は、妊娠前の世帯が対象で既存の
  `child_stage` の区分に当てはまらないため、`child_stage` を空にした。空はどの
  プロフィールでも表示される扱いなので、「高校生」を選んだ人にも表示される。
  ステージに「妊娠を考えている」を新設するかは、プロフィールUIの変更になるため今回は見送り。
- 制度数が39件になり、「設計判断の理由」で書いた「40件を超えたあたりで折りたたみか検索が
  必要になる」目安に達した。**これ以上追加する場合は、先に折りたたみ・検索の設計書を起こす。**
