# [YYYY-MM-DD] 機能名 設計書

**設計者**: （例：Claude Code）
**実装担当**: （例：Antigravity）
**状態**: 未着手 / 実装中 / 実装完了・レビュー待ち / 完了

## 概要

（何のための機能か、なぜ必要かを2-3文で）

## データ構造

（新規・変更するJSON/DBスキーマ等。フィールド名・型・必須/任意を明記）

```
例:
{
  "id": "string (必須)",
  "title": "string (必須)",
  "note": "string (任意)"
}
```

## 画面・API仕様

（画面がある場合はレイアウト・状態遷移。APIがある場合はエンドポイント・パラメータ・レスポンス形式）

## タスク一覧

各タスクは半日程度で完了できる単位に分割し、完了条件を明記する。

| # | タスク | 完了条件 |
|---|---|---|
| 1 | （例：JSONスキーマの追加） | （例：`sample.json`に新フィールドを追加し、既存の読み込み処理がエラーなく動く） |
| 2 | | |

## 設計判断の理由（なぜこの設計にしたか）

（他の案と比較してこれを選んだ理由。後で見直すときのために短く）

## 実装時の疑問点・ブロッカー（実装担当が記入）

（設計書と食い違う点、判断に迷った点があればここに追記し、無断で仕様を変えずに報告する。
 解決したら日付と結論を追記して残す）

---
※ このファイルをコピーして `docs/agent-notes/design-specs/YYYY-MM-DD_機能名.md` として保存してください。
※ 実装が完了したら「状態」を更新し、`CURRENT_STATUS.md` にも反映すること。
※ 大きな設計判断は完了後に `decisions-log/` にも記録すると経緯が追いやすくなります。
