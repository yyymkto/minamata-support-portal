# 現在の状態（最終更新：2026-08-15 15:30 by Claude）

> このファイルは常に「今の状態」を反映するよう **上書き更新** します。
> 過去の経緯を追いたい場合は `decisions-log/` を見てください。

## 今どのフェーズか

子育て領域のデータ設計・UIプロトタイプが完成し、本番`src/index.html`への
統合作業に入る段階。統合作業はAntigravity主導で進行中。

## 確定している設計方針

- 収集と表示は分離：動的データ（`life_info.json`、新着情報）と
  静的データ（`child_support_base.json`、制度基礎情報）の二層構造
- ストック制度は「常時展開のカテゴリ見出し一覧」方式
  （クリック絞り込みは不採用、詳細は decisions-log/2026-08-15_stock-view-no-click.md）
- 技術スタックは静的HTML＋Tailwind CDN＋Vanilla JS維持（React等は導入しない）
- 属性登録は個人情報を持たず、ローカルストレージのみで疑似実装する方針（未着手）

## 現在のファイル構成（想定）

```
public/data/
├── life_info.json              # 動的データ（既存、自動更新）
├── child_support_base.json     # 静的データ・子育て制度（28件、手動更新）
└── concern_mapping.json        # こまりごとマッピング（16件、4カテゴリ）
src/
└── index.html                  # 統合先（新着情報＋使える制度の2入口を持たせる）
```

## 進行中のタスク

| タスク | 担当 | 状態 |
|---|---|---|
| src/index.htmlへの統合（fetch方式への書き換え含む） | Antigravity | 着手中 |
| 要確認だった6件のURL調査 | Claude | 完了 |
| 検診・予防接種等、未踏カテゴリの完全な洗い出し | 未着手 | - |
| 疑似属性登録（ローカルストレージ） | 未着手 | - |

## 未決定の論点（次に議論すべきこと）

- 新着情報／使える制度、2つの入口をどう共存させるか（タブ？セグメント？）
- デフォルト表示はどちらにするか（新着情報を維持する案が有力）
- 「使える制度」が子育て領域限定であることをUI上でどう明示するか

## 直近の変更履歴

- 2026-08-15: 要確認6件のURL調査完了、データに反映
- 2026-08-15: ストック制度UIを「クリック式」→「常時展開の一覧」に変更
- 2026-08-15: 子育て制度データを12件→28件に拡充
