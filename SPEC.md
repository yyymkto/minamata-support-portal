# みなまた くらしナビ（非公式）仕様書

最終更新：2026-08-26

> この文書は、サイトの現状を正確に把握するための仕様書です。
> 「このサイトをどう使ってもらうか（利用促進・認知戦略等）」を検討する際の
> インプットとして作成しました。技術的な変更履歴は
> `docs/agent-notes/CURRENT_STATUS.md` と `docs/agent-notes/decisions-log/` を、
> セットアップ手順は `README.md` を参照してください。

## 1. これは何か

水俣市公式サイト等の情報を自動収集・手動キュレーションし、世代・困りごと別に
整理して届ける**非公式**の静的サイトです。GitHub Pagesでホストされ、ビルド
ステップなしの単一HTML（`src/index.html`）＋ JSON データという最小構成です。

**非公式であること**はUI上に明記しており、「申請前には必ず一次情報（公式サイト）
を確認する」旨も表示しています。市の公式発表物ではなく、個人（吉野さん）が
Claude・Antigravityという複数のAIエージェントと協業して開発・運用しています。

## 2. 情報構成：2タブ構成

画面上部のタブで、性質の異なる2種類の情報を切り替えます（「フローとストックの
分離」という設計方針、詳細は `decisions-log/2026-08-15_stock-view-no-click.md`）。

### 2-1. 「最新のお知らせ」タブ（フロー）

- データソース：`public/data/life_info.json`（`scripts/update_data.py`が毎朝
  自動更新）
- 対象：**全世代・全立場**（子育て、学生・若者、現役世代、高齢者、
  障がいのある方、事業者向け、全世代の7分類）
- 収集元：水俣市公式サイトの新着情報・防災サイト・スポーツ情報（RSS）、
  みなまた観光物産協会のイベント情報・お知らせ（計5ソース）
- 各記事にGemini APIで世代タグ・ジャンルタグ・要約を自動付与
- 世代・ジャンルでの絞り込み、キーワード検索が可能
- 並び順：実際の公開日が判明している記事を優先し、その中で新しい順。
  公開日が不明な記事は「確認日（このサイトが見つけた日時）」で代替表示

### 2-2. 「子育て支援制度」タブ（ストック）

- データソース：`public/data/child_support_base.json`（人手で年1〜数回更新）
  ＋ `public/data/concern_mapping.json`（困りごと分類の定義）
- 対象：**子育て世帯限定**（水俣市の子育て支援制度のみ。他世代向けの制度は
  現状このタブには含まれない）
- 「困りごと」チップ（16種類）を4カテゴリ（妊娠・出産／お金・手当・医療費／
  預ける・学校／特別なサポート）に整理し、常時展開＋上部ジャンプナビで一覧表示
- 「あてはまるものを選ぶと絞り込める」プロフィール機能（子どものステージ・
  世帯状況をローカルストレージに保存し、合致しない制度を自動的に除外。
  個人情報はサーバーに送信されない）
- 表現方針（2026-08-26変更）：「あなたが使える制度を探す」という利用者目線の
  表現ではなく、「水俣市の子育て支援制度」という中立的な紹介表現を採用。
  水俣市内在住者・市外の読者どちらが読んでも自然に伝わることを意図し、
  一方で「〜たら」「〜ますよ」といった誘導的・勧誘的な言い回しは避けている

## 3. データと自動化の仕組み

| 項目 | 内容 |
|---|---|
| フロントエンド | 単一HTML（`src/index.html`）、Tailwind CDN、Vanilla JS。ビルドステップなし |
| 生活情報の自動収集 | `scripts/update_data.py`、毎朝7:00 JST（GitHub Actions） |
| 判定・タグ付け | Gemini API（`gemini-3.1-flash-lite`、無料枠）。関連性判定・タグ付け・要約生成 |
| ストック情報の監視 | `scripts/monitor_stock.py`、毎週月曜8:00 JST。制度ページの更新・リンク切れを検知しIssue起票（データは自動更新しない） |
| デプロイ | GitHub Pages、pushまたは収集完了のたびに自動デプロイ |

自動収集パイプラインには、運用初日（2026-08-24〜25）に発生した実障害への
対応として以下を実装済み：
- 文字コードの誤判定によるタイトル文字化けの修正
- Gemini APIのレート制限（RPM/RPD）対応：指数バックオフでの再試行、
  ペース調整、恒久的な上限到達時に処理を打ち切るサーキットブレーカー
- 判定済みURLの記録による重複判定の排除（2回目以降は新着分のみ判定、
  API消費を大幅削減）
- 記事ページ本体からの正確な公開日取得（一覧ページに日付が無い記事が大半のため）

## 4. 現状のスナップショット（2026-08-26時点）

- 「最新のお知らせ」：193件
  - ソース内訳：水俣市新着情報159、防災サイト23、観光物産協会イベント4、
    スポーツ情報3、観光物産協会お知らせ3、その他1
  - 世代タグの内訳：全世代138、現役世代31、事業者向け26、高齢者24、
    学生・若者12、子育て・妊娠期9、障がいのある方9
    （**注**：約7割が「全世代」タグとなっており、「世代別に整理する」という
    コンセプトに対して、実際には世代を横断する内容が多数を占めている）
- 「子育て支援制度」：27制度、16の困りごと、4カテゴリ
- Gemini判定済みURL（累積・重複排除の対象）：219件

## 5. できていること（強み）

- フロー／ストックの分離が設計・実装・運用の全レベルで一貫している
- 自動収集パイプラインが実運用に耐える堅牢性を持つ（文字化け・レート制限・
  重複判定・エラー通知まで対応済み。2026-08-24〜25の1日強で作り込み）
- ストック情報の「サイレント更新」を検知する仕組みがあり、手動更新の
  取りこぼしに気づける
- ユーザーの状況に応じた絞り込み（疑似属性登録）が、個人情報を持たずに
  ローカルストレージのみで実現できている
- コンテンツの表現が誘導的・勧誘的でなく、中立的な事実描写に統一されている

## 6. できていないこと・制約（弱み・オープンな論点）

- **「子育て支援制度」タブは子育て世帯限定**。サイト全体としては
  「世代・立場ごとに整理」を掲げているが、ストック情報（具体的な制度を
  探せる部分）は子育て領域にしか存在しない。高齢者・障がいのある方・
  事業者向けなどの制度は「最新のお知らせ」で偶然拾われるもの以外は存在しない
- 「最新のお知らせ」の公開日は、一覧ページの構造上、記事によって取得できる
  ものとできないものがある（水俣市公式サイト系は記事ページから取得可能、
  みなまた観光物産協会は取得手段が無い）
- 本番デプロイ（GitHub Pages）後の実地確認・動作確認がまだ十分でない
- Gemini APIは無料枠を利用しており、収集ソースを大きく増やす場合は
  レート制限（RPM15・RPD500、モデルにより異なる）を意識する必要がある
- ユーザー属性登録はローカルストレージのみ（端末をまたいだ同期や、
  能動的な通知機能などは無い）

## 7. 戦略検討のための論点（たたき台）

- 現状「全世代」タグが7割を占める実態を踏まえると、「世代別に整理された
  サイト」という打ち出し方は看板と中身にややギャップがある。打ち出し方を
  見直すか、実際に世代別の切り分けを強化するか
- 「子育て支援制度」タブの子育て限定という制約は、対象読者を明示的に
  絞ったサービスとして訴求するか、他世代への拡張を目指すかで戦略が変わる
- 2026-08-26に「水俣に来たらこんな支援が受けられる」という移住検討者も
  意識した見せ方への転換を試みた（誘導的な表現は避けつつ）。この路線を
  他のコンテンツ（例：移住・定住関連の新着情報の扱い方）にも広げるかは未定
- 非公式サイトであることの認知（信頼性の担保）と、利用促進（見つけてもらう
  こと）を両立させる導線をどう設計するか
